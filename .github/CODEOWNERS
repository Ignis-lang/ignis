# Committed gate baselines.
#
# These directories hold recorded compiler answers, one file per case. A diff
# in one is a semantic change — the compiler now schedules a drop somewhere
# else, or accepts a program it used to reject — and regenerating them is a
# single command, which makes it the easy way to turn a red gate green.
#
# This entry has no effect until branch protection on `main` requires review
# from code owners; see BOOTSTRAP.md, "Drop-schedule baselines (G7)".
test_cases/e2e/ok/__drop_schedules__/ @0xErwin1
