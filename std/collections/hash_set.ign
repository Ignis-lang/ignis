//! # HashSet
//!
//! Re-export of the deterministic set built in `hash_map.ign`.

import _ from "std::collections::hash_map";

export HashSet from "./hash_map";

function __hashSetModuleRoot(): void {
  return;
}
