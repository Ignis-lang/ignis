import String from "std::string";

/// Trait for values that can render themselves as an owned `String`.
///
/// Types marked with `@implements(Display)` must provide
/// `display(&self): String`.
export trait Display {
  display(&self): String;
}
