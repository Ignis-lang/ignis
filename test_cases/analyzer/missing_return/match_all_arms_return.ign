// Test: a statement-position match whose every arm returns is a terminator

enum Shape {
  CIRCLE,
  SQUARE,
}

function sides(shape: Shape): i32 {
  match (shape) {
    Shape::CIRCLE -> {
      return 0;
    },
    Shape::SQUARE -> {
      return 4;
    },
  };
}
