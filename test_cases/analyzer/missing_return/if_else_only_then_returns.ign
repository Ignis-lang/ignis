// Test: if/else where only THEN branch returns should produce MissingReturnStatement

function getValue(x: bool): i32 {
  if (x) {
    return 1;
  } else {
    let y: i32 = 2;
    // Missing return in else branch
  }
}
