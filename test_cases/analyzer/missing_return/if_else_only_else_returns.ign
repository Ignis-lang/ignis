// Test: if/else where only ELSE branch returns should produce MissingReturnStatement

function getValue(x: bool): i32 {
  if (x) {
    let y: i32 = 1;
    // Missing return in then branch
  } else {
    return 2;
  }
}
