// Test: Nested if/else where all paths return should be valid (no error)

function getValue(x: bool, y: bool): i32 {
  if (x) {
    if (y) {
      return 1;
    } else {
      return 2;
    }
  } else {
    return 3;
  }
}
