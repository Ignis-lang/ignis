// Test: a match with an arm that does not return cannot guarantee a return,
// should produce MissingReturnStatement

enum Shape {
  CIRCLE,
  SQUARE,
}

function sides(shape: Shape): i32 {
  match (shape) {
    Shape::CIRCLE -> {
      return 0;
    },
    Shape::SQUARE -> {},
  };
}
