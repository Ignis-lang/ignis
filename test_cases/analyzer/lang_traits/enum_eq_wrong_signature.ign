// Test: enum Eq requires equals(&self, other: &Self): boolean
// Expected: ERROR A0133 - wrong signature

trait Eq {
}

@implements(Eq)
enum WrongEq {
  READY,

  equals(&self, other: i32): boolean {
    return other == 1;
  }
}

function main(): i32 {
  return 0;
}
