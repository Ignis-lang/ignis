// Test: Record with a String field is NOT structurally Copy
// Expected: use-after-move error on `h`

record Handle {
  public name: string;
  public id: i32;
}

function main(): i32 {
  let h: Handle = Handle { name: "hello", id: 1 };
  let h2: Handle = h;
  return h.id;
}
