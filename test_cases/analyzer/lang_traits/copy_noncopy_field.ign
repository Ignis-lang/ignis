// Test: @implements(Copy) on a record with a non-Copy field
// Expected: ERROR A0148 - field 'inner' has non-Copy type 'Owned'

@implements(Drop)
record Owned {
  public id: i32;
  drop(&mut self): void { return; }
}

@implements(Copy)
record Handle {
  public inner: Owned;
  public id: i32;
}

function main(): i32 {
  return 0;
}
