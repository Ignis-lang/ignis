// Test: @implements(Copy) on a record with a non-Copy field
// Expected: ERROR A0148 - field 'name' has non-Copy type 'string'

@implements(Copy)
record Handle {
  public name: string;
  public id: i32;
}

function main(): i32 {
  return 0;
}
