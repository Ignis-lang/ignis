// Test: Record with correct Drop implementation
// Expected: no errors

@implements(Drop)
record Resource {
  handle: i32;

  drop(&mut self): void {
    return;
  }
}

function main(): i32 {
  let r: Resource = Resource { handle: 42 };
  return 0;
}
