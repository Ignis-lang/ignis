// Test: Record with explicit @implements(Copy)
// Expected: no errors, variable can be used after assignment

@implements(Copy)
record Point {
  public x: i32;
  public y: i32;
}

function main(): i32 {
  let p: Point = Point { x: 1, y: 2 };
  let q: Point = p;
  return p.x + q.y;
}
