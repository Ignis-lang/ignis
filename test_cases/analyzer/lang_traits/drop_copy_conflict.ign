// Test: Drop and Copy conflict
// Expected: ERROR A0131 - cannot implement both Drop and Copy

@implements(Drop, Copy)
record Bad {
  x: i32;

  drop(&mut self): void {
    return;
  }
}

function main(): i32 {
  return 0;
}
