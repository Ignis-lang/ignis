// Test: drop method has wrong signature (takes extra param)
// Expected: ERROR A0133 - wrong signature

@implements(Drop)
record WrongSig {
  x: i32;

  drop(&mut self, extra: i32): void {
    return;
  }
}

function main(): i32 {
  return 0;
}
