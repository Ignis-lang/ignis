import Vector from "std::vector";
import Option from "std::option";

function main(): i32 {
  let mut vec: Vector<i32> = Vector::init<i32>();
  vec.push(42);
  vec.push(100);
  
  let len: u64 = vec.length();
  
  let popped: Option<i32> = vec.pop();
  
  vec.clear();
  
  return 0;
}
