// Test: for-of loop with fixed-size array, by value (Copy type)

function main(): void {
    let arr: i32[3] = [1, 2, 3];
    let mut sum: i32 = 0;

    for (let x of arr) {
        sum = sum + x;
    }

    return;
}
