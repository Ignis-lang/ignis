// Test: for-of loop with fixed-size array, by immutable reference

function main(): void {
    let arr: i32[3] = [1, 2, 3];
    let mut sum: i32 = 0;

    for (let x: &i32 of arr) {
        sum = sum + *x;
    }

    return;
}
