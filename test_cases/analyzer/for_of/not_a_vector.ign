// Test: for-of over non-vector type (should produce error)

function main(): void {
    let x: i32 = 42;

    for (let y of x) {
        // This should produce an error: for-of expects vector type
    }

    return;
}
