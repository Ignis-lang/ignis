[toolchain]
base_header = "runtime/ignis_rt.h"
base_header_quoted = true
include_dirs = [".", "runtime"]

[modules]
compile = "compile/mod.ign"
path = "path/mod.ign"

[auto_load]
modules = ["path"]

[compile_only]
modules = ["compile"]
