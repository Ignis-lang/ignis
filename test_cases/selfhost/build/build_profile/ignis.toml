[package]
name = "resolver-build-profile"
version = "0.1.0"

[ignis]
std = true
std_path = "./std"
runtime_path = "./std/runtime"

[build]
source_dir = "src"
entry = "main.ign"
out_dir = "build/out"
opt_level = 2
debug = true
cflags = ["-Wall", "-fno-omit-frame-pointer"]
