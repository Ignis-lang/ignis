function apply(f: (i32) -> i32, x: i32): i32 {
    return f(x);
}

function main(): i32 {
    let offset: i32 = 30;
    let add_offset = (n: i32): i32 -> n + offset;
    return apply(add_offset, 12);
}
