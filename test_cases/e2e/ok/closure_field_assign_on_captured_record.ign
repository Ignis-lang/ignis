record Point {
    public x: i32;
}

function main(): i32 {
    let mut p: Point = Point { x: 1 };
    let bump = (): void -> { p.x = 41; };
    bump();

    if (p.x == 41) {
        return 0;
    }

    return p.x;
}
