@implements(Drop)
record Named {
  public tag: i32;
  public value: i32;

  drop(&mut self): void {
      return;
  }
}

function main(): i32 {
    let n: Named = Named { tag: 1, value: 42 };
    return n.value;
}
