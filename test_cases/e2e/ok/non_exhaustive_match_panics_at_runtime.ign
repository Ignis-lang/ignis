// e2e: allow-leak
enum Color {
  RED,
  GREEN,
  BLUE,
}

function classify(c: Color): i32 {
  return match (c) {
    Color::RED -> 1,
    Color::GREEN -> 2,
  };
}

function main(): i32 {
  return classify(Color::BLUE);
}
