enum Option {
  Some(i32),
  None,
}

function main(): i32 {
  let unwrapOr = (o: Option, fallback: i32): i32 -> {
    return match (o) {
            Option::Some(x) -> x,
            Option::None -> fallback,
        };
  };

  return unwrapOr(Option::Some(42), 0);
}
