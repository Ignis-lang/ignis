@implements(Drop)
record Resource {
    public tag: i32;

    drop(&mut self): void {
        return;
    }
}

function main(): i32 {
    let mut r: Resource = Resource { tag: 10 };
    r.drop();
    r = Resource { tag: 77 };
    return r.tag;
}
