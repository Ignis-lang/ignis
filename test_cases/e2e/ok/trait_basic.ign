trait Greetable {
    greet(&self): i32;
}

@implements(Greetable)
record Person {
    public age: i32;

    greet(&self): i32 {
        return self.age;
    }
}

function main(): i32 {
    let p: Person = Person { age: 42 };
    return p.greet();
}
