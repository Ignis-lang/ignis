@implements(Drop)
record Pair {
  public a: i32;
  public b: i32;

  drop(&mut self): void {
      let _sum: i32 = self.a + self.b;
      self.a = 0;
      self.b = 0;
      return;
  }
}

function main(): i32 {
    let p: Pair = Pair { a: 10, b: 32 };
    return p.a + p.b;
}
