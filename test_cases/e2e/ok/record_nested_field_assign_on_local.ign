record Inner {
    public value: i32;
}

record Outer {
    public inner: Inner;
}

function main(): i32 {
    let mut o: Outer = Outer { inner: Inner { value: 1 } };
    o.inner.value = 42;
    return o.inner.value - 42;
}
