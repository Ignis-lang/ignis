enum Box {
    VALUE(u64),
    EMPTY,
}

function main(): i32 {
    let boxed: Box = Box::VALUE(9);

    match (boxed) {
        Box::VALUE(hash) -> {
            return hash as i32;
        },
        Box::EMPTY -> {
            return 1;
        },
    };

    return 0;
}
