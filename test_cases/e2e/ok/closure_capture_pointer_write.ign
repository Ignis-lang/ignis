function main(): i32 {
    let mut value: i32 = 10;
    let p: *mut i32 = (&mut value) as *mut i32;
    let write_through = (x: i32): void -> {
        @write<i32>(p, x);
    };
    write_through(42);
    return @read<i32>(p);
}
