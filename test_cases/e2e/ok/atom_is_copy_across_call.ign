function id(x: atom): atom {
    return x;
}

function main(): i32 {
    let original: atom = :foo;
    let copied: atom = id(original);

    if (original == copied) {
        return 7;
    }

    return 0;
}
