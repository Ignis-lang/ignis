function twice(x: i32): i32 {
    return x * 2;
}

function main(): i32 {
    // a + b |> f  ->  a + (b |> f)
    // 10 + 3 |> twice = 10 + 6 = 16
    let result: i32 = 10 + 3 |> twice;
    return result;
}
