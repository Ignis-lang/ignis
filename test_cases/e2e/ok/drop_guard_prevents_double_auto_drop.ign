@implements(Drop)
record Counter {
    public value: i32;

    drop(&mut self): void {
        self.value = self.value + 1;
        return;
    }
}

function main(): i32 {
    let mut c: Counter = Counter { value: 0 };
    c.drop();
    return 0;
}
