function apply(@noescape f: (i32) -> i32, x: i32): i32 {
    return f(x);
}

function main(): i32 {
    let mut val: i32 = 10;
    let inc = (n: i32): i32 -> {
        val = val + 1;
        return n + val;
    };
    return apply(inc, 5);
}
