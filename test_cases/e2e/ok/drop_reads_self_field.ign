@implements(Drop)
record Resource {
  public tag: i32;

  drop(&mut self): void {
      let _t: i32 = self.tag;
      return;
  }
}

function main(): i32 {
    let r: Resource = Resource { tag: 42 };
    return r.tag;
}
