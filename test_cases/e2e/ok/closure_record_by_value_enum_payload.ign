enum Action {
  Compute((i32) -> i32),
  Skip,
}

function main(): i32 {
  let action: Action = Action::Compute((n: i32): i32 -> n * 2);

  return match (action) {
        Action::Compute(f) -> f(21),
        Action::Skip -> 0,
    };
}
