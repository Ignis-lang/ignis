function readValue(state: &mut State): i32 {
    return state.value;
}

record State {
    public value: i32;
}

function main(): i32 {
    let mut state: State = State { value: 7 };

    return readValue(&mut state);
}
