record Counter {
    public value: i32;

    addTo(&mut self, x: i32): i32 {
        self.value = self.value + x;
        return self.value;
    }
}

function main(): i32 {
    let mut c: Counter = Counter { value: 10 };
    return 5 |> c.addTo();
}
