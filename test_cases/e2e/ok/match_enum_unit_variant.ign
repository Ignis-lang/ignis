enum Color {
    Red,
    Green,
    Blue,
}

function main(): i32 {
    let color: Color = Color::Green;
    return match (color) {
        Color::Red -> 1,
        Color::Green -> 2,
        Color::Blue -> 3,
    };
}
