// A closure with no captures has no environment to place anywhere, so crossing a
// return neither heap-allocates nor frees anything.
function makeConst(): (i32) -> i32 {
    return (x: i32): i32 -> x + 1;
}

function main(): i32 {
    let f = makeConst();

    return f(41);
}
