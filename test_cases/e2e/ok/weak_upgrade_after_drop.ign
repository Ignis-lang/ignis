// e2e: std
import Rc, Weak from "std::rc";

function main(): i32 {
    let mut a: Rc<i32> = Rc::new<i32>(99);
    let w: Weak<i32> = a.downgrade();

    a.drop();

    if (w.upgrade().isNone()) {
        return 0;
    }

    return 1;
}
