@implements(Drop)
record Logger {
  public tag: i32;
  public level: i32;

  drop(&mut self): void {
      return;
  }
}

function main(): i32 {
    let l: Logger = Logger { tag: 1, level: 3 };
    return l.level;
}
