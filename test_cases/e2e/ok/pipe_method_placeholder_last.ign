record Math {
    public base: i32;

    sub(&self, a: i32, b: i32): i32 {
        return self.base + a - b;
    }
}

function main(): i32 {
    let m: Math = Math { base: 100 };
    return 7 |> m.sub(3, _);
}
