const add: (i32, i32) -> i32 = (a: i32, b: i32): i32 -> a + b;
const mul: (i32, i32) -> i32 = (a: i32, b: i32): i32 -> {
    return a * b;
};

function main(): i32 {
    let r1 = add(2, 3);
    let r2 = mul(4, 5);
    return r1 + r2;
}
