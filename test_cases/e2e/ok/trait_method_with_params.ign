trait Addable {
    addTo(&self, other: i32): i32;
}

@implements(Addable)
record Counter {
    public count: i32;

    addTo(&self, other: i32): i32 {
        return self.count + other;
    }
}

function main(): i32 {
    let c: Counter = Counter { count: 30 };
    return c.addTo(12);
}
