enum Option {
    Some(i32),
    None,
}

enum Wrapper {
    Wrap(Option),
    Empty,
}

function main(): i32 {
    let value: Wrapper = Wrapper::Wrap(Option::Some(42));

    return match (value) {
        Wrapper::Wrap(Option::Some(x)) -> x,
        _ -> 0,
    };
}
