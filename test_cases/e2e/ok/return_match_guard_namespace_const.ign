namespace Config {
    const LIMIT: i32 = 10;
    const ZERO: i32 = 0;
}

function classify(x: i32): i32 {
    return match (x) {
        k if k == Config::ZERO -> 1,
        k if k == Config::LIMIT -> 2,
        _ -> 0,
    };
}

function main(): i32 {
    return classify(10);
}
