@lang(try)
enum Result<T, E> {
    OK(T),
    ERROR(E),
}

function parse(ok: boolean): Result<i32, i32> {
    if (ok) {
        return Result::OK(7);
    }

    return Result::ERROR(1);
}

function main(): i32 {
    let value: i32 = parse(true) else {
        return 1;
    };

    return value;
}
