// e2e: std
import Rc from "std::rc";

function main(): i32 {
    let mut a: Rc<i32> = Rc::new<i32>(10);

    let Option::SOME(ptr) = a.getMut() else {
        return 1;
    };

    @write<i32>(ptr, 42);

    return *a.get();
}
