@implements(Drop)
record Handle {
  public id: i32;

  drop(&mut self): void {
      return;
  }
}

enum MaybeHandle {
    Some(Handle),
    None
}

function main(): i32 {
    let m: MaybeHandle = MaybeHandle::Some(Handle { id: 1 });
    return 0;
}
