@implements(Drop)
record Inner {
  public tag: i32;

  drop(&mut self): void {
      return;
  }
}

record Outer {
  public inner: Inner;
  public code: i32;
}

function main(): i32 {
    let o: Outer = Outer {
        inner: Inner { tag: 1 },
        code: 42
    };
    return o.code;
}
