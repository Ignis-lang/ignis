// e2e: err
// The predicate inside a negation is still resolved, so an unknown one is
// named rather than silently folding to a value.

@configFlag(!@bogus("x"))
function gated(): u64 {
  return 1;
}

function main(): i32 {
  return 0;
}
