// e2e: err
// A `@configFlag` annotation whose condition is not a predicate expression is
// reported where it is written, not carried into the declaration it gates.

@configFlag(!)
function gated(): u64 {
  return 1;
}

function main(): i32 {
  return 0;
}
