// e2e: err
@implements(Drop)
enum Resource {
    Active(i32),
    Idle

    drop(&mut self): void {
        return;
    }
}

function consume(r: Resource): i32 {
    return 0;
}

function main(): i32 {
    let mut r: Resource = Resource::Active(10);
    consume(r);
    r.drop();
    return 0;
}
