// e2e: err
@implements(Drop)
record Owned {
    public id: i32;

    drop(&mut self): void {
        return;
    }
}

function main(): i32 {
    let value: Owned = Owned { id: 1 };
    let list: Owned[1] = [value];
    let again: Owned = value;
    return 0;
}
