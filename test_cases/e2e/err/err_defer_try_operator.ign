// e2e: err
@lang(try)
enum Result<T, E> {
    Ok(T),
    Err(E),
}

function fallible(): Result<i32, i32> {
    return Result::Ok(1);
}

function main(): Result<i32, i32> {
    defer fallible()!;
    return Result::Ok(0);
}
