// e2e: err
@implements(Drop)
record Handle {
    public id: i32;

    drop(&mut self): void {
        return;
    }
}

function main(): i32 {
    let h: Handle = Handle { id: 1 };
    let h2: Handle = h;
    return h.id;
}
