// e2e: err
// Generated by scripts/gen_ownership_corpus.py. Do not edit by hand.
//
// Pruning rule 7: tuple patterns parse, but no tuple type can be spelled, so an owning tuple scrutinee cannot exist.

@implements(Drop)
record Tracked {
  public id: i32;

  drop(&mut self): void {
    return;
  }
}

function makePair(): (Tracked, Tracked) {
  return (Tracked { id: 1 }, Tracked { id: 2 });
}

function main(): i32 {
  let pair: (Tracked, Tracked) = makePair();

  return match (pair) {
    (left, right) -> left.id + right.id,
  };
}
