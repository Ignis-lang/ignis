// e2e: err
@implements(Drop)
record Named {
    public id: i32;

    drop(&mut self): void {
        return;
    }
}

function consume(n: Named): i32 {
    return 0;
}

function main(): i32 {
    let n: Named = Named { id: 1 };
    let x: i32 = consume(n);
    return consume(n);
}
