import { println } from "std:io";

let hello: string = "Hello";
let world: string = "World";

println(hello + " " + world);

let num1: int = 12;
let num2: int = 12;

println(num1);
println(num2);

let result: int = num1 + num2;
println(result);

println("Resutl: " + (result > 15).toString());

println("Or" + (true || false).toString());
println("And" + (true && false).toString());

let mut a: int;

a = 1;
