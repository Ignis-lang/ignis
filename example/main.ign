import println from "std::io";
import allocate, deallocate from "std::memory";

function main(): i32 {
  let mut value: *mut i32 = allocate(16) as *mut i32;
  *value = 42;

  println("Hello, World!");

  deallocate(value); // Drop
  return *value; // Error
}
