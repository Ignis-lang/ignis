import println from "std::io";
import toString from "std::string";

extern const TEST: i32;
extern function testFunction(): void;

// Test
export function add(a: &mut i32, b: i32): i32 {
  return *a + b;
}

/**
 * Main function
 */
function main(): void {
  println("Hello World");
  let mut value: i32 = 42;
  let mut var: i32 = 0;

  let result: i32 = add(&mut value, var);
  println(toString(result));
  return;
}
