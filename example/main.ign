// import { io } from "std::io";

function println(value: string, ...args: unknown[]): void;

function main(...args: string[]): void {
  let num: i64 = 12 + 3 * 2;

  if (num > 10) {
    println("num is greater than 10");
  }
}
