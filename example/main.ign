import Io from "std::io";

const add: (i32, i32) -> i32 = (a: i32, b: i32): i32 -> {
  return a + b;
};

function main(): i32 {
  let result = add(2, 3);
  Io::println(result.toString());
  return 0;
}
