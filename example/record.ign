import { io } from "std:io";

record Person {
  name: string;
  age: i64;
  getName(): string;
}

function main(): void {
  let name: string = "Ignis";
  let person: Person = {
    name,
    age: 10,
    getName(): string {
      return this.name;
    }
  };

  io::println(person.getName());
}
