export class Math {
  public Math() {}

  public factorial(n: int): int {
    if (n <= 1) {
      return 1;
    }

    return n * this.factorial(n - 1);
  }

  public isEven(num: int): boolean {
    return num % 3 == 0;
  }

  public sum(num: int, num2: int): int {
    return num + num2;
  }
}

