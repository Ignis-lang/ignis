import { println } from "std:io";

function main(): void {
  let name: string = "John";

  let message: string = match name with
                        | "John" -> "Hi John!"
                        | "Mary" -> { return "Hi Mary!"; }
                        | n -> "Hi " + n + "!";

  println(message);
}
