=== HIR ===


--- Definitions ---

fn sizes(): u64  [export]
  body:
    Block : void
      statements:
        Let(size) : void
          init:
            SizeOf(u32) : u64
        Let(align) : void
          init:
            AlignOf(u32) : u64
        Return : never
          Binary(Add) : u64
            left:
              Variable(size) : u64
            right:
              Variable(align) : u64

--- Variable Initializers ---

align: u64 = alignOf(u32)
size: u64 = sizeOf(u32)
