fn guarded
  local count: u64
  local stride: u64
  local total: u64
  entry:
    store count
    store stride
    store total
    store local3
    -> branch
  sc_right:
    store local3
    -> goto
  sc_merge:
    -> return
fn fallback
  local flag: boolean
  local count: u64
  entry:
    store flag
    store count
    store local2
    -> branch
  sc_right:
    store local2
    -> goto
  sc_merge:
    -> return
